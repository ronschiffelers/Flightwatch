{
  "name": "flightwatch-proxy",
  "version": "1.0.0",
  "description": "Minimal proxy server for Flightwatch — forwards requests to Anthropic API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
